[package]
name = "sickle_math"
version = "0.2.1"
edition = "2021"

[features]
dev = ["bevy/dynamic_linking"]

[dependencies]
bevy = { version = "0.14" }
serde = { version = "1.0", features = ["derive"] }
